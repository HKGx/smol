

fn factorial(n: int) int do
    mut let out = 1
    for v in range(1, n) do
        let out = out * v
    end
    out
end

fn rec_factorial(n: int) int do
    if n = 1: 1
    else if n = 0: 1
    else:
        n * rec_factorial(n-1)
end

factorial(10)
rec_factorial(10)